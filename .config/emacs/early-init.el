;;; early-init.el -- Early customizations

;;; Commentary:

;;; Inhibit package.el in favor of straight.el.
;;; Perform any other early setup.

;;; Code:

(setq package-enable-at-startup nil)

;;; early-init.el ends here
